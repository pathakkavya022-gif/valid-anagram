ALGORITHM:
STEP->1:intialize two strings.
STEP->2:check length of two strings.
STEP->3:use for loop (int i=0;str[i]!='\0';i++)
STEP->4:write freq[str1[i]-'a']++;(check string(1) characters ).
STEP->5:freq[str2[i]-'a']--;(check string(2) characters).
STEP->6:use another for loop for frequency of characters
STEP->7:if(freq[i]!=0) 
STEP->8:print not valid anagram.
STEP->9:ELSE (print valid anagram).
STEP->10:return 0;
